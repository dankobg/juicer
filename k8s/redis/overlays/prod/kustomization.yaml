kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

resources:
  - ../../base

namespace: prod

# namePrefix: prod-

generators:
  - redis-prod-gen.yaml

configMapGenerator:
  - name: redis-conf
    envs:
      - redis-prod.conf
