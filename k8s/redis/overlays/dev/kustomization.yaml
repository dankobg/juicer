kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

resources:
  - ../../base

namespace: dev

# namePrefix: dev-

generators:
  - redis-dev-gen.yaml

configMapGenerator:
  - name: redis-conf
    envs:
      - redis-dev.conf
